

var mask1 = new OriDomi('#mask-1', {
          hPanels:         2,     // number of panels when folding left or right (vertically oriented)
          speed:           500,
          touchSensitivity: 0.55,
          shadingIntesity: 0.5,    // lessen the shading effect
          shading:         'none' // change the shading type
        });
        mask1.accordion(100, 'top');

var mask2 = new OriDomi('#mask-2', {
          hPanels:         2,     // number of panels when folding left or right (vertically oriented)
          speed:           500,
          touchSensitivity: 0.55,
          shadingIntesity: 0.5,    // lessen the shading effect
          shading:         'none' // change the shading type
        });
        mask2.accordion(100, 'top');

var mask3 = new OriDomi('#mask-3', {
          hPanels:         2,     // number of panels when folding left or right (vertically oriented)
          speed:           500,
          touchSensitivity: 0.55,
          shadingIntesity: 0.5,    // lessen the shading effect
          shading:         'none' // change the shading type
        });
        mask3.accordion(100, 'top');

var mask4 = new OriDomi('#mask-4', {
          hPanels:         2,     // number of panels when folding left or right (vertically oriented)
          speed:           500,
          touchSensitivity: 0.55,
          shadingIntesity: 0.5,    // lessen the shading effect
          shading:         'none' // change the shading type
        });
        mask4.accordion(100, 'top');

var mask5 = new OriDomi('#mask-5', {
          hPanels:         2,     // number of panels when folding left or right (vertically oriented)
          speed:           500,
          touchSensitivity: 0.55,
          shadingIntesity: 0.5,    // lessen the shading effect
          shading:         'none' // change the shading type
        });
        mask5.accordion(100, 'top');


var mask6 = new OriDomi('#mask-6', {
          hPanels:         2,     // number of panels when folding left or right (vertically oriented)
          speed:           500,
          touchSensitivity: 0.55,
          shadingIntesity: 0.5,    // lessen the shading effect
          shading:         'none' // change the shading type
        });
        mask6.accordion(100, 'top');


var mask7 = new OriDomi('#mask-7', {
          hPanels:         2,     // number of panels when folding left or right (vertically oriented)
          speed:           500,
          touchSensitivity: 0.55,
          shadingIntesity: 0.5,    // lessen the shading effect
          shading:         'none' // change the shading type
        });
        mask7.accordion(100, 'top');


